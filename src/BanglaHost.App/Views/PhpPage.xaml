<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BanglaHost.App.Views.PhpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BanglaHost.App.Views"
    Loaded="Page_Loaded">
    
    <ScrollViewer>
    <StackPanel Padding="24" Spacing="16">
        <StackPanel Spacing="2">
            <TextBlock Text="PHP Tools" FontSize="26" FontWeight="SemiBold"/>
            <TextBlock Opacity="0.7" Text="Manage Xdebug, php.ini, and logs for each installed PHP version."/>
        </StackPanel>

        <TextBlock Text="Installed PHP Versions" FontSize="16" FontWeight="SemiBold"/>
        <Border BorderThickness="1" CornerRadius="8" Padding="4"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel>
                <TextBlock x:Name="EmptyVers" Text="No PHP versions installed yet." Opacity="0.6" Padding="10" Visibility="Collapsed"/>
                <ListView x:Name="VersList" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:PhpToolRow">
                            <Grid Padding="8,6" ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{x:Bind Version}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind StatusText}" Opacity="0.7" VerticalAlignment="Center" Foreground="{x:Bind StatusColor}"/>
                                
                                <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="6">
                                    <ToggleSwitch OffContent="Xdebug Off" OnContent="Xdebug On" IsOn="{x:Bind IsXdebugOn, Mode=TwoWay}" Toggled="Xdebug_Toggled" Tag="{x:Bind Version}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <Button Content="Edit php.ini" Tag="{x:Bind Version}" Click="EditIni_Click"/>
                                    <Button Content="Error Log" Tag="{x:Bind Version}" Click="ErrorLog_Click"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>

        <TextBlock Text="Composer" FontSize="16" FontWeight="SemiBold" Margin="0,16,0,0"/>
        <Border BorderThickness="1" CornerRadius="8" Padding="16"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Content="Update Composer" Click="UpdateComposer_Click"/>
                <ProgressRing x:Name="ComposerBusy" IsActive="False" Width="20" Height="20" VerticalAlignment="Center"/>
                <TextBlock x:Name="ComposerStatus" VerticalAlignment="Center" Opacity="0.8"/>
            </StackPanel>
        </Border>
    </StackPanel>
    </ScrollViewer>
</Page>
